import { useState } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'
import './App.css'

/*
  Adding Styles: 

  In React, you specify a CSS class with "className". It works the same way as the HTML "class" attribute:

      <img className="avatar" />

Then you write the CSS rules for it in a separate CSS file. 
    
    // In your CSS file
    .avatar {
      border-radius: 50%; 
    }

  React does not prescribe how you add CSS files. In the simplest case. you'll add a "<link>" tag to your HTML. 
  If you use a build too or a framework, consult its documentation to learn how to add a CSS file to your project. 
*/

function App() {

  return (
    <div className="App">
      
    </div>
  )
}

export default App
